<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Default Theme-->
    <style name="Theme.DrawingApplication" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Theme -->
    <style name="Theme.DrawingApplication.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.DrawingApplication</item>
    </style>

</resources>
